{
	"dependencies": {
		"luaparse": "^0.3.1"
	},
	"devDependencies": {
		"@types/express": "^5.0.6",
		"@types/node": "^25.0.10",
		"esbuild": "0.25.9",
		"expect": "^30.1.2",
		"typescript": "^5.9.3"
	},
	"scripts": {
		"build": "node --experimental-strip-types Build.ts --once",
		"build-prod": "node --experimental-strip-types Build.ts --once --prod",
		"test": "node --enable-source-maps --test test.ts",
		"typecheck": "tsc --watch --project tsconfig.json --preserveWatchOutput",
		"typecheck-once": "tsc --project tsconfig.json"
	},
	"type": "module"
}
